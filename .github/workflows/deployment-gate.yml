name: PLATO Deployment Gate
on: pull_request
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: SuperInstance/plato-room-deployment-approval@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
